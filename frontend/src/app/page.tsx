'use client';  // Client component for hooks

export default function Home() {
  return (
    <div style={{ padding: '20px' }}>
      <h1 style={{ fontSize: '24px', marginBottom: '20px' }}>FeeDelegate Bridge</h1>
      <p>Test page - no errors if this loads.</p>
    </div>
  );
}